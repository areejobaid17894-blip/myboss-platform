stages:
  - verify

verify-docs:
  stage: verify
  image: alpine:3.20
  script:
    - test -f docs/README.md
    - test -f docker/docker-compose.demo.yml
    - test -f scripts/deploy-demo-server.sh
    - echo "Platform structure OK"
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
